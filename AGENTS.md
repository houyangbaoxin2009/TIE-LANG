# 用户偏好

## 语言

- 全程中文交流
- 代码注释、提交消息用中文

## 工作流

1. 用户自行检查
2. 确认后让 AI 提交并推送

## 关注点

- 编译零错误

- 代码模块化

## 代码风格

- 即时保存配置（用户操作即写文件）
- 写多余注释
- 功能与方法一一对应

## 文档风格（skill / 教程类文档）

- Markdown 表格用**列宽对齐**格式（`| 头部声明 | 角色 |` + 对齐分隔行），不用紧凑 `|---|---|`
- 用户向文档**不写内部阶段编号**（如 `（S2.2，已实现）`、`（S1.3）`）——阶段代号是编译器开发内部标识，对应用开发者无意义
- **区分受众**：语言用户文档只讲语言用法（语法/标准库/CLI/工程化）；编译器内部流程（探针/自举/回归）只写进编译器开发文档
- **README 是导航不是细节仓库**：CLI 选项表、包管理器子命令、文件角色细节等下沉到 docs/（如 docs/cli.md），README 只留工程入口 + 快速开始 + 文档索引 + 工程结构 + 入口链接
- **NEW.md 只写本版全新功能**：亮点表只列本版新增（延续项整行删除），不带「来源」列（不标 preview.2 延续/本版全新）；本版无关章节（标准库/快速上手）整章删掉，引导去 README/CHANGELOG

## 文档

- 维护 README.md、CHANGELOG.md

## 沟通风格

- 指令简短直接
